{
  "buildCommand": "yarn build",
  "outputDirectory": "dist",
  "devCommand": "yarn dev",
  "installCommand": "yarn install",
  "framework": "nextjs",
  "rewrites": [
    { "source": "/admin/(.*)", "destination": "/api/[...payload]" },
    { "source": "/api/(.*)", "destination": "/api/[...payload]" }
  ]
}